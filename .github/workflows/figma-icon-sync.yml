on:
  workflow_dispatch:
  push:

permissions:
  packages: write
  contents: write
  pull-requests: write

jobs:
  sync:
    uses: wanteddev/wds/.github/workflows/figma-icon-sync.yml@feature/sh031224/platform-sync
    with:
      platform: ios
    secrets: inherit

**ENUM**

# `Select.Render`

```swift
public enum Render
```

variant가 multiple일 때 컴포넌트에 표시될 내용의 형태를 결정하는 열거형입니다.
- text: 선택된 항목 텍스트만 표시
- chip: 선택된 항목을 칩(chip) 형태로 표시

## Cases
### `text`

```swift
case text
```

### `chip`

```swift
case chip
```

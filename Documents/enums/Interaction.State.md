**ENUM**

# `Interaction.State`

```swift
public enum State
```

상호작용의 상태를 정의합니다.

- `.normal`: 기본 상태 (아무 상호작용 없음)
- `.hovered`: 호버 상태 (마우스 오버)
- `.focused`: 포커스 상태 (키보드 포커스)
- `.pressed`: 누름 상태 (클릭/터치)

## Cases
### `normal`

```swift
case normal
```

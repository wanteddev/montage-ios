Instance Method

# pushBadge(_:) 

푸시 알림 표시 뱃지를 아바타에 추가합니다.

```swift
@MainActor
func pushBadge(_ pushBadge: Bool = true) -> Avatar
```

## Parameters 

pushBadge

뱃지 표시 여부 (기본값: true)

## Return Value 

수정된 아바타 인스턴스

## Discussion 

푸시 뱃지는 사용자(.person) 아바타에만 적용 가능합니다.


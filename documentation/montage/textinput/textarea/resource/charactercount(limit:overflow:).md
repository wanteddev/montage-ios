Case

# TextInput.TextArea.Resource.characterCount(limit:overflow:) 

문자 수 카운터

```swift
case characterCount(
    limit: Int? = nil,
    overflow: Bool = false
)
```

## Parameters 

limit

최대 문자 수 제한 (nil인 경우 제한 없음)

overflow

최대 문자 수 초과 허용 여부

